---
tipo: readme
estado: activo
---
# docs/historial/ — lo cumplido: planes ejecutados e instantáneas superadas del hub

Se lee para saber por qué algo es como es, no para saber cómo se usa (NORMATIVA §15.6). Nada de aquí es
normativo; lo vigente está en `../` y en `../decisiones.md`.

| documento | qué es |
|---|---|
| `course-redesign-plan.md` | el plan que unificó `talk/` y `teching/` en `cursos/` (implementado el 2026-07-09, PR #10): análisis, jerarquía curso → edición → sesión, preservación de URL por `aliases`, estado de implementación |
